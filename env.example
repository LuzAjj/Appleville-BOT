# REQUIRED
SESSION_TOKEN=eyJhbGciOi...   # value of cookie "session-token"
NEXTAUTH_SESSION_TOKEN=       # some setups use this name instead (if you see it, put the value here too)

# OPTIONAL but nice to have
CSRF_TOKEN=                   # value of cookie "__Host-authjs.csrf-token" (if present)

# (rare) if your cookie names differ, you can set RAW_COOKIE below and leave the 3 vars above empty:
# RAW_COOKIE=session-token=...; __Host-authjs.csrf-token=...;
